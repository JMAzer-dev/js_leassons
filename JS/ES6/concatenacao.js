const name = 'Joao';
const age = '28';
var estudando = ''

if (estudando){
    console.log(`${name} de ${age} está estudano`)
} else {
    console.log(`${name} de ${age} não está estudando`)
}
